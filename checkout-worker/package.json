{
  "name": "tor-checkout",
  "version": "1.0.0",
  "private": true,
  "description": "Temples of Refuge — Stripe embedded-checkout backend (Cloudflare Worker).",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
