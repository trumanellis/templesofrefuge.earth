{
  "name": "tor-checkout",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Temples of Earth — Stripe embedded-checkout backend (self-hosted Node service behind Caddy).",
  "scripts": {
    "start": "node src/node-server.js",
    "dev:worker": "wrangler dev",
    "deploy:worker": "wrangler deploy"
  },
  "dependencies": {
    "nodemailer": "^6.9.16"
  },
  "devDependencies": {
    "wrangler": "^3.90.0"
  }
}
